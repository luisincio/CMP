@using Cmp02.Entities;
@model ClienteClover

@using (Ajax.BeginForm("Registro2", "Cliente", new AjaxOptions { UpdateTargetId = "divCliente", HttpMethod = "POST", InsertionMode = InsertionMode.Replace }, new { @id = "frmEditarCliente" }))
{
    <div id="divCliente">
        @Html.Partial("_EditarEmpresa2", Model);
    </div>
}
