@using Cmp02.Entities;
@model IEnumerable<ColegiadoMin>

<div class="panel-group" id="accordion">
    @using (Ajax.BeginForm("Listar_Empresa", "Comprobante", new AjaxOptions { UpdateTargetId = "divModal", HttpMethod = "POST", InsertionMode = InsertionMode.Replace }, new { @id = "frmModal" }))
    {
        <div class="panel panel-default">
            <div id="collapse1" class="panel-collapse collapse in">
                <div class="panel-body">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                <label for="vValor" class="control-label col-md-3 input-sm">Buscar por Nombres o DNI:</label>
                                <div class="col-md-9">
                                    <div class="input-group">
                                        @Html.TextBox("vValor", null, new { @class = "form-control input-sm toupper next", @placeholder = "Ingrese nombre, apellido o número documento", @maxlength = "100" })
                                        <span class="input-group-btn">
                                            <button type="submit" class="btn btn-primary btn-sm">Buscar</button>
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="panel panel-default">
            <div id="collapse2" class="panel-collapse collapse in">
                <div id="divModal" class="panel-body">
                    @Html.Partial("_ListarPersonaDetalle", Model)
                </div>
            </div>
        </div>
    }
</div>
