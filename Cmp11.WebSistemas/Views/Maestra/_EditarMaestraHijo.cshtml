@model Cmp02.Entities.Tb_Maestras

@using (Ajax.BeginForm("EditarHijo", "Maestra", new AjaxOptions { UpdateTargetId = "divMaestra", HttpMethod = "POST", InsertionMode = InsertionMode.Replace }, new { @id = "frmEditarMaestra" }))
{
    <div id="divMaestra">
        @if (Model.Opcional == "OK")
        {
            <script>
                modal.modal("hide");
                $("#frmDetalle").submit();
            </script>
        }
        else
        {
            @Html.Partial("_EditarMaestraHijo2", Model)
        }
    </div>
}
